# Required for AI chart scanner
LOVABLE_API_KEY=

# Required for license caching / device binding (optional — app still works without)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Render sets PORT automatically; default 10000
PORT=10000
